# Combined Pipeline – requirements.txt
# Python 3.12  |  venv: combined_pipeline/venv/
#
# Cài đặt:
#   cd combined_pipeline
#   source venv/bin/activate
#   pip install -r requirements.txt

# ── VnCoreNLP ─────────────────────────────────────────────────────────────────
py_vncorenlp>=0.1.4

# ── PyTorch (load .pt local) ──────────────────────────────────────────────────
torch

# ── HuggingFace Transformers (tokenizer + pipeline NER) ──────────────────────
# NOTE: Phải dùng 4.x — transformers 5.x không tương thích với model ElectraAttention cũ
transformers>=4.30,<4.40

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn

# ── Scientific computing ──────────────────────────────────────────────────────
numpy

# ── NLP bổ trợ ────────────────────────────────────────────────────────────────
underthesea

# ── Kilo AI / OpenAI SDK (phân nhóm chủ đề — MiniMax via Kilo AI) ────────────
openai>=1.0
python-dotenv

# ── Gradio UI ─────────────────────────────────────────────────────────────────
gradio

# ── Backend API ──────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
PyPDF2
